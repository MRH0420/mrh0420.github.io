---

import { siteConfig } from "../config";
---

<div
	id="config-carrier"
	data-hue={siteConfig.themeColor.hue}
	data-theme-toggleable={String(siteConfig.theme.toggleable)}
	data-default-theme={siteConfig.theme.mode}
>
</div>
